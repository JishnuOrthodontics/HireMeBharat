version: '3.8'

services:
  # API Gateway
  # Secrets (MONGODB_URI, FIREBASE_*) come from `.env` via env_file — not ${VAR} interpolation,
  # which can truncate or corrupt multiline PEM keys.
  api-gateway:
    image: localhost:5000/hiremebharat-api-gateway:latest
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      - PORT=3001
      - HOST=0.0.0.0
      - NODE_ENV=production
      - FRONTEND_URL=https://hiremebharat.com
      - MONGODB_DB=hiremebharat
      - UPSTREAM_AUTH=http://api-auth:3002
      - UPSTREAM_EMPLOYEE=http://api-employee:3003
      - UPSTREAM_EMPLOYER=http://api-employer:3004
      - UPSTREAM_ADMIN=http://api-admin:3005
    restart: unless-stopped

  # Auth Service (handles public routes)
  api-auth:
    image: localhost:5000/hiremebharat-api-auth:latest
    env_file:
      - .env
    environment:
      - PORT=3002
      - HOST=0.0.0.0
      - NODE_ENV=production
      - FRONTEND_URL=https://hiremebharat.com
      - MONGODB_DB=hiremebharat
    restart: unless-stopped

  # Employee Service
  api-employee:
    image: localhost:5000/hiremebharat-api-employee:latest
    env_file:
      - .env
    environment:
      - PORT=3003
      - HOST=0.0.0.0
      - NODE_ENV=production
      - FRONTEND_URL=https://hiremebharat.com
      - MONGODB_DB=hiremebharat
    restart: unless-stopped

  # Employer Service
  api-employer:
    image: localhost:5000/hiremebharat-api-employer:latest
    env_file:
      - .env
    environment:
      - PORT=3004
      - HOST=0.0.0.0
      - NODE_ENV=production
      - FRONTEND_URL=https://hiremebharat.com
      - MONGODB_DB=hiremebharat
    restart: unless-stopped

  # Admin Service
  api-admin:
    image: localhost:5000/hiremebharat-api-admin:latest
    env_file:
      - .env
    environment:
      - PORT=3005
      - HOST=0.0.0.0
      - NODE_ENV=production
      - FRONTEND_URL=https://hiremebharat.com
      - MONGODB_DB=hiremebharat
    restart: unless-stopped

  # Local Docker Registry (for GCP deployment)
  registry:
    image: registry:2
    ports:
      - "5000:5000"
    volumes:
      - ./registry-data:/var/lib/registry
    environment:
      - REGISTRY_AUTH=htpasswd
      - REGISTRY_AUTH_HTPASSWD_PATH=/auth/htpasswd
      - REGISTRY_AUTH_HTPASSWD_REALM=Registry Realm
    command:
      - /bin/sh -c
      - |
        htpasswd -Bbn $REGISTRY_USER $REGISTRY_PASSWORD > /auth/htpasswd &&
        exec /etc/docker/registry/config.yml
    restart: unless-stopped

