# Security

If Google Cloud, GitHub, MongoDB, Firebase, or Cloudflare credentials were shared in chat, email, or tickets, **rotate them immediately** and enable **two-factor authentication** on the Google account used for GCP.

Never commit `.env` files or private keys to the repository.

